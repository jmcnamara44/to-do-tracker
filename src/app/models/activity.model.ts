export class Activity {
  constructor(public name: string, public hoursPracticed: {}, public hoursToComplete: number, public goalCompletionDate: string, public notes: string, public dateCreated: string) { }
}
