export class Activity {
  constructor(public name: string, public hoursPracticed: number, public hoursToComplete, public goalCompletionDate: string, public notes: string, public dateCreated: string) { }
}
